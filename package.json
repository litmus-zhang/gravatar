{
  "name": "gravatar",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gravatar",
    "create-local": "graph create --node http://localhost:8020/ gravatar",
    "remove-local": "graph remove --node http://localhost:8020/ gravatar",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gravatar"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
